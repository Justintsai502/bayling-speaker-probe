# Stages 2-5. BayLing-Duplex itself: git clone https://github.com/BayLing-Models/BayLing-Duplex && pip install -e BayLing-Duplex
# (its requirements.txt brings torch, torchaudio, transformers>=4.45, scipy, etc.)
numpy
scipy
soundfile
librosa        # mp3 voice prompts
speechbrain>=1.0
transformers==4.57.3   # BayLing remote code reads config.max_length, removed in v5; also what its config.json records
matplotlib
tiktoken             # GLM-4 tokenizer (BayLing remote code); not in BayLing requirements.txt
